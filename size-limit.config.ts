import type { Config } from "size-limit";

// Per-package budgets live in each package's local size-limit.config.ts via
// libraryBudget() from @realistdb/size-limit-config. This root config is a
// stub until the first publishable package ships.
const config: Config = [];

export default config;
