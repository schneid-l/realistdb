# @realistdb/tsconfig-config

Shared TypeScript presets for the realistdb monorepo.

- `@realistdb/tsconfig-config/base` — base (strict-strict) options, no emit.
- `@realistdb/tsconfig-config/lib` — extends base, adds output paths + declaration emission for publishable packages.

Not published. Internal use only.
